namespace mod04{
    let add:(i:number,j:number)=>number;
    add= function (i,j) {
        return i+j;
    };
    console.log(add(10,20));

    let sub:(i:number,j:number)=>number;
    sub=(i,j)=>i-j;
    console.log(sub(30,40));
}